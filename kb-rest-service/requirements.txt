# Core framework
fastapi==0.136.0
uvicorn[standard]==0.32.0
starlette==1.0.0
gunicorn==26.0.0

# Data validation and configuration
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv==1.2.2
typing-extensions>=4.0.0

# Database
sqlalchemy==2.0.51
asyncpg==0.30.0
psycopg2-binary==2.9.12
pymongo==4.17.0
motor==3.7.0
greenlet==3.1.1

# Graph database
neo4j==6.2.0

# Caching
redis==5.0.1

# Authentication and security
PyJWT[crypto]==2.13.0
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
certifi==2026.6.17
msal==1.37.0

# Logging
structlog==24.1.0

# HTTP clients
aiohttp==3.14.1
requests==2.34.2

# ============================================================================
# Cloud Provider Dependencies
# ============================================================================
# Install based on your deployment target:
#   - Azure only:     Install all dependencies (default)
#   - AWS only:       Install all dependencies (AWS packages will be used)
#   - GCP only:       Install all dependencies (GCP packages will be used)
#   - Multi-cloud:    Install all dependencies (all packages will be available)
#
# Alternatively, use optional extras from pyproject.toml:
#   pip install "kb-rest-service[aws]"      # Azure + AWS
#   pip install "kb-rest-service[gcp]"      # Azure + GCP
#   pip install "kb-rest-service[all-clouds]"  # All providers
# ============================================================================

# --- Azure Cloud Provider (default, always installed) ---
azure-functions==1.18.0
azure-storage-blob==12.30.0
azure-storage-queue==12.9.0
azure-servicebus==7.12.1

# --- AWS Cloud Provider (for AWS Lambda, S3, SQS deployments) ---
# Required for: AWS Lambda runtime, AWS S3 storage, AWS SQS queue, AWS Textract OCR
boto3>=1.34.0,<2.0

# --- GCP Cloud Provider (for GCP Cloud Functions, Cloud Storage deployments) ---
# Required for: GCP Cloud Functions runtime, GCP Cloud Storage, GCP Document AI OCR
google-cloud-storage>=2.14.0,<3.0
google-cloud-documentai>=2.20.0,<3.0

# --- Azure OCR (Document Intelligence) ---
# Required for: Azure Document Intelligence OCR (chat file extraction fallback)
# Can be used from any cloud provider (cross-cloud API call)
azure-ai-documentintelligence==1.0.2

# ============================================================================
# RAG and AI
# ============================================================================
lightrag-hku==1.4.16
numpy==2.3.5

# ============================================================================
# Document text extraction (shared/text_extraction)
# ============================================================================
# Used for chat file-context extraction and indexing pipeline
python-docx==1.2.0
PyPDF2==3.0.1
pdfplumber==0.11.0

# ============================================================================
# Internal packages
# ============================================================================
git+https://github.com/Coforge-forgeX/forgexpackages.git@main#egg=common-adapters
